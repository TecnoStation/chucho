import * as React from 'react';
import type { ReactNode } from 'react';
export interface TreeNodeProps {
    /**
     * The node label
     * */
    label: React.ReactNode;
    className?: string;
    children?: ReactNode;
    id: "CardNode";
}
declare function TreeNode({ children, label, className, id }: TreeNodeProps): JSX.Element;
export default TreeNode;
